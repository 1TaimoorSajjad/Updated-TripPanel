import { PlacesDirective } from './places.directive';

describe('PlacesDirective', () => {
  it('should create an instance', () => {
    const directive = new PlacesDirective();
    expect(directive).toBeTruthy();
  });
});
